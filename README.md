```sql
DELETE FROM QAPORTAL.TEST_STEP
WHERE TEST_ID NOT IN (
  SELECT TEST_ID FROM QAPORTAL.TEST_RUN
  UNION
  SELECT TEST_ID FROM QAPORTAL.TEST_PARAM
  UNION
  SELECT TEST_ID FROM QAPORTAL.TEST_TAG
);
```
