SELECT * FROM TEST_STEP_RUN WHERE TEST_RUN_ID = 145804;

SELECT * 
FROM TEST_STEP 
WHERE ID IN (
    SELECT TEST_STEP_ID FROM TEST_STEP_RUN WHERE TEST_RUN_ID = 145804
);

SELECT * 
FROM TEST_STEP_DATA 
WHERE TEST_STEP_ID IN (
    SELECT TEST_STEP_ID FROM TEST_STEP_RUN WHERE TEST_RUN_ID = 145804
);
